export interface DropdownContent {
  icon: React.ElementType;
  label: string;
  href?: string;
}

export interface NoticeDropdownContent {
  icon: React.ElementType;
  label: string;
  href?: string;
}

export interface SidebarContent {
  icon: React.ElementType;
  label: string;
  href?: string;
}