# 快速开始指南

## 前置要求

- Node.js >= 16
- MongoDB >= 4.4
- pnpm (推荐) 或 npm

---

## 1. 安装依赖

```bash
pnpm install
```

---

## 2. 配置环境变量

在项目根目录创建 `.env` 文件:

```env
# 应用配置
PORT=3000

# 数据库配置
MONGODB_URI=mongodb://localhost:27017/nebula-nest

# JWT配置
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRES_IN=7d

# 密码加密轮次
BCRYPT_SALT_ROUNDS=10
```

> ⚠️ **重要**: 生产环境必须修改 `JWT_SECRET` 为强随机字符串!

---

## 3. 启动MongoDB

### 方式1: 本地MongoDB
```bash
# Windows
net start MongoDB

# Linux/Mac
sudo systemctl start mongod
```

### 方式2: Docker
```bash
docker run -d -p 27017:27017 --name mongodb mongo:latest
```

---

## 4. 启动应用

### 开发模式
```bash
npm run dev
```

### 生产模式
```bash
# 编译
npm run build

# 启动
npm run start:prod
```

应用将运行在 `http://localhost:3000`

---

## 5. 测试接口

### 使用curl测试

#### 1) 注册用户
```bash
curl -X POST http://localhost:3000/api/users/register \
  -H "Content-Type: application/json" \
  -d "{\"username\":\"testuser\",\"email\":\"test@example.com\",\"password\":\"Test123\"}"
```

#### 2) 登录
```bash
curl -X POST http://localhost:3000/api/users/login \
  -H "Content-Type: application/json" \
  -d "{\"email\":\"test@example.com\",\"password\":\"Test123\"}"
```

**保存返回的token!**

#### 3) 获取当前用户信息
```bash
curl -X GET http://localhost:3000/api/users/profile \
  -H "Authorization: Bearer YOUR_TOKEN_HERE"
```

### 使用Postman或其他工具

1. 导入API集合
2. 设置环境变量 `baseUrl` = `http://localhost:3000`
3. 测试各个接口

---

## 6. 接口列表

| 方法 | 路径 | 说明 | 需要认证 |
|------|------|------|----------|
| POST | `/api/users/register` | 用户注册 | ❌ |
| POST | `/api/users/login` | 用户登录 | ❌ |
| GET | `/api/users/profile` | 获取当前用户 | ✅ |
| GET | `/api/users` | 获取所有用户 | ✅ |
| GET | `/api/users/:id` | 获取指定用户 | ✅ |

---

## 常见问题

### Q1: 无法连接数据库

**解决方法**:
1. 检查MongoDB是否启动
2. 验证 `.env` 中的 `MONGODB_URI` 配置
3. 检查防火墙设置

### Q2: Token认证失败

**解决方法**:
1. 确认请求头格式: `Authorization: Bearer <token>`
2. 检查token是否已过期
3. 验证 `JWT_SECRET` 配置一致

### Q3: 密码验证不通过

**解决方法**:
- 密码必须至少6个字符
- 必须包含字母和数字
- 例如: `Test123`, `Hello123`

### Q4: 端口被占用

**解决方法**:
修改 `.env` 中的 `PORT` 为其他可用端口

---

## 下一步

- 📖 查看 [API使用指南](./API使用指南.md) 了解详细的接口文档
- 🔧 查看 [环境配置说明](./环境配置说明.md) 了解配置选项
- 📝 查看 [认证系统改进说明](./认证系统改进说明.md) 了解系统架构

---

## 开发提示

### 热重载
```bash
npm run dev
```
修改代码后会自动重启

### 代码格式化
```bash
npm run format
```

### 代码检查
```bash
npm run lint
```

### 编译TypeScript
```bash
npm run build
```

---

## 生产部署建议

1. **环境变量**
   - 使用强JWT密钥
   - 配置生产数据库
   - 设置合适的CORS策略

2. **数据库**
   - 启用MongoDB认证
   - 配置数据库备份
   - 使用连接池

3. **安全**
   - 启用HTTPS
   - 配置速率限制
   - 添加日志监控

4. **性能**
   - 使用PM2或其他进程管理器
   - 启用集群模式
   - 配置Redis缓存

---

## 技术支持

如有问题,请查看:
- [MongoDB学习指南.md](../MongoDB学习指南.md)
- [mongoose学习指南.md](../mongoose学习指南.md)
- [NestJS核心学习文档.md](../NestJS核心学习文档.md)

祝你开发愉快! 🚀

